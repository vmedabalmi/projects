{
  "name": "@patentproject/expiration-pipeline",
  "version": "0.1.0",
  "description": "Expiration pipeline — enriches patent expiration data with urgency labels, summaries, and lookahead windows",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@patentproject/expiration": "file:../expiration",
    "pino": "^8.17.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": ["<rootDir>/tests"]
  }
}
